{
  "name": "coolshift-platform",
  "version": "1.0.0",
  "description": "Smart Energy Optimization Platform - CoolShift Hackathon",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run client\"",
    "server": "cd backend && python main.py",
    "client": "cd src/client && npm run dev",
    "build": "cd src/client && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
